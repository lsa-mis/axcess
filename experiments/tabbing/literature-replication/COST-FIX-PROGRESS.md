# COST-FIX progress

- [1] Preconditions: no prior progress file; `derived/kafe_matrix.jsonl` has 52 lines; git status shows only BRIEF-COST-FIX.md and two derived logs untracked. Files changed: none.
- [2] Recomputed from `artifacts/kafe_results_to_reproduce.csv`, taking the `Detection` column at face value. All 60, per visible ctrl node: Detection median 25.8 / min 1.1 / max 225.8 / 60 of 60 under 300 / pooled 20.0; CrawlKnfg+CrawlPcnfg median 3823.9 / min 329.4 / max 15363.9 / 0 of 60 / pooled 3117.8; all seven median 22683.3 / min 6138.8 / max 125850.8 / 0 of 60 / pooled 23000.8; mean full pipeline 19.12 min. Scored 39 (pooled the way the report pools it, sum/sum): Detection pooled 25.7, median 35.8, 39 of 39 under 300; crawls pooled 3601.0, median 4333.2, 0 of 39; all seven pooled 23229.8, median 28370.8, min 6138.8, max 81741.2, 0 of 39. Matches the brief's table. Files changed: this file.
- [3] STOPPED: premise check failed. `Detection` (and `Localization`) look like running totals, not per-subject times. Sorted by value they follow alphabetical run order, with groupon and mozilla run last. That gives 58 of 59 non-decreasing steps, against 27 to 30 drops for each crawl column. corr(raw Detection, run rank) = 0.956; corr(raw Detection, ctrl nodes) = 0.05. The per-subject increments track page size (corr with all nodes 0.70; largest are bowiestate 263, canon 173, cnn 161). Differenced: per subject median 10 ms, mean 26.3; per ctrl pooled 0.53 ms (all 60) or 0.51 ms (scored 39), median 0.3. So the brief's "Detection only = 25.7 ms" figure is itself about 50x too high. The full-pipeline figure barely moves: Detection adds 59.7 s summed at face value, about 1.6 s differenced. No report, generator or derived file edited; nothing regenerated; nothing committed. Waiting for a decision.
- [4] Resumed after the 21:30 update. `derived/kafe_matrix.jsonl` still 52 lines. Wrote `tools/fetch_kafe_output.py` (reuses `fetch_kafe_all.parse_embedded` and `drivemeta.fetch`; 20 MB cap). `resolve` listed `KAFE_output` (`1hKV7KoaMA2Lsgzly9-3cwA-QTU4X-ZM4`): 60 subject folders plus 6 loose root CSVs (`subjectsFin.csv`, `successfullyProcessedDetection.csv`, `successfullyProcessedLocalization.csv`, three `totalSummary_*.csv`), which were not fetched. `fetch` downloaded all 180 wanted files (60 × `execTime.csv`, `execTimeDetection.csv`, `resultsSubjectStats.csv`) with 0 failures, 34,639 B total. Files changed: `tools/fetch_kafe_output.py` (new), this file.
- [5] Kept local: all files, plus `_index.json` and `_manifest.json`, are under `artifacts/kafe_output/`. `git check-ignore` matches the existing `artifacts/` rule in this experiment's `.gitignore`, and `git status --untracked-files=all` lists none of them. No new ignore rule was needed. Files changed: this file.
- [6] Gaps: none. All 60 subjects (the same 60 names as the results CSV, no extras) have all three files, each non-empty and parsing as two-column key/value rows with one identical key set per file type (14 phases; `01-Type1Detection`, `02-Type2Detection`; three node counts). Denominator cross-check against `resultsSubjectStats.csv`: `Size of All Visible Ctrl Nodes` agrees on 59/60; **resellerratings is 50 per-subject vs 146 in the CSV** (and visible nodes 164 vs 345). Other node-count differences, none in the ctrl denominator: boostmobile all 414/415, indiegogo all 343/359 and visible 32/34, instagram visible 123/124, resellerratings all 465/459, usgsgov all 997/995. Files changed: this file.
- [7] Phase mapping, chosen by brute force (every 1–4 phase subset of the 16 per-subject timers against each CSV column, ranked by median relative error) and matching the names: `InitializeProxyKnfg`=00; `ExtractNodesKnfg`=01+04; `CrawlKnfg`=02+03+05; `InitializeProxyPcnfg`=06; `ExtractNodesPcnfg`=07+11; `CrawlPcnfg`=08+09+10 (08 `BuildEFEsPcnfg` is 0–10 ms). 12/13 `TotalConstruct*` are not CSV columns: they equal 05 (resp. 10) + 318–730 ms (resp. 310–3,720), so they overlap other timers and are excluded from sums. Agreement per-subject/CSV across 60 (median ratio; within ±5% / ±10%; pooled sum ratio): IPK 1.007, 17/28, 1.028; ENK 0.991, 38/44, 1.035; CK 1.008, 45/52, 1.028; IPP 1.019, 16/31, 0.978; ENP 1.001, 38/38, 0.989; CP 1.006, 37/47, 1.023. Outliers >25%: 15, 8, 4, 12, 16, 8 subjects (worst indiegogo CrawlKnfg 5.96×, dell ExtractNodesPcnfg 3.85×, gizmodo CrawlPcnfg 3.12×). So the crawl/extract columns agree well pooled and at the median, but not subject-for-subject everywhere, and the proxy-init columns loosely; together with the node-count differences, the per-subject files look like a separate run of the same pipeline, not the same record. `Detection` maps onto nothing: best subset error 99.9% (08 alone); vs Type 1 median ratio 3.48 (1/60 within ±5%); vs Type1+Type2 11.8 (0/60). Drops in alphabetical order: CSV `Detection` 2, CSV `Localization` 2, `01-Type1Detection` 26, `02-Type2Detection` 26, crawl columns 27–30; corr with alphabetical rank: CSV `Detection` 0.93, Type 1 0.02. Files changed: this file.
- [8] Figures, ms per visible ctrl node, denominator = results CSV ctrl nodes. Type 1 detection: all 60 median 104.3 / min 6.2 / max 636.1 / pooled 65.5 / 53 of 60 under 300; scored 39 median 128.5 / min 7.2 / max 636.1 / pooled 84.4 / 34 of 39. Crawl phases (02+03+05+08+09+10): all 60 median 3,866.2 / 330.8 / 22,359.7 / pooled 3,201.3 / 0 of 60; scored 39 median 4,638.4 / 672.4 / 16,133.9 / pooled 3,639.1 / 0 of 39. Full pipeline (phases 00–11 + Type 1): all 60 median 24,004.7 / 5,182.0 / 130,006.9 / pooled 23,261.2 / 0 of 60; scored 39 median 27,682.0 / 7,344.1 / 68,265.3 / pooled 22,747.5 / 0 of 39. Mean full pipeline per subject 19.33 min (paper 19.22; CSV columns gave 19.12). Type 2 (median 252.6, pooled 163.7 all 60) is counted in none of these; adding it moves full-pipeline pooled to 23,424.9 (60) / 22,959.2 (39). Type 1 over 300: fandango, live, snapchat, ubersignup, waze (scored), plus indiegogo, spotify. Type 1 is 0.28% of the full pipeline; proxy init 40.1%, in-crawl node extraction 40.6%. With per-subject denominators (only resellerratings changes) the scored-39 pooled figures become Type 1 90.0, crawl 3,881.2, full 24,260.9; medians shift by resellerratings' rank only; no under-300 count changes. The brief's spot checks reproduce (4shared 80.0, adorama 266.9, bbc 129.0). Files changed: this file.
- [9] Generator `tools/kafe_matrix.py`: (a) `assemble` now splits each row's abstentions inside the 39 scored subjects by KAFE label (`unknown_positive`, `unknown_negative`) and adds `recall_strict` = TP / 24; `recall` stays decided-only. (b) KAFE's cost now comes from the per-subject logs (`kafe_logged_timing`: Type 1, crawls, full pipeline); the KAFE row's `ms/button`/`ms/subject` are the full pipeline, `ms covers` names it and gives Type 1 alone. The CSV `Detection` figures survive only under `results_csv_detection_column` with a caveat, plus a computed `detection_column_evidence`. (c) `KAFE-MATRIX.md` table gains `unk pos`, `unk neg`, `strict recall`; `recall` → `recall (decided)`, `F1` → `F1 (decided recall)`; ms prose rewritten; KAFE cost table added. `KAFE-MATRIX-REPORT.md` Control 1 and method note rewritten. Ran `assemble`: vs HEAD, every existing field of every Axcess row is identical in both summaries; only the KAFE row's `ms_total`, `ms_per_button` (25.7 → 22747.5), `ms_per_subject` (1012.7 → 897652.7) and `ms_covers` changed. Invariants hold on all 48 rows: TP+FN+unk pos = 24, FP+TN+unk neg = 15, abst. = 13 + unk pos + unk neg. C12: unk pos 3, strict recall 79.2%, decided 90.5% (matches the manager). Proxy init is 44.6% of the full pipeline on the 39. Files changed: `tools/kafe_matrix.py`, `derived/kafe_matrix_summary.json`, `derived/kafe_matrix_summary_permissive.json`, this file.
- [10] Ran `report` (after two wording fixes: "alphabetical (run) order" → alphabetical order, since groupon/mozilla ran last; "reproduces 19.22" → "comes to 19.33, against their paper's 19.22"). `KAFE-MATRIX.md` cell check vs HEAD, columns matched by header: 3 cells changed in pre-existing columns, all in the KAFE row (`ms/button` 25.7 → 22747.5, `ms/subject` 1012.7 → 897652.7, `ms covers`); every TP/FP/FN/TN/abst./precision/recall/F1/Axcess ms cell identical. New columns: strict recall differs from decided recall on 39 of 48 Axcess rows; strict = decided on D4, D7, U-D7, C6, D9+S4u, D9-noS4, D10a, D10a+base, D10b (and KAFE). Controls JSON: only control 1's timing block changed. `KAFE-MATRIX-REPORT.md`: only timestamp, Control 1 timing prose/JSON, and the method note changed. `tests/test_capdiag.py` 8 passed. JSONL still 52 lines. Files changed: `KAFE-MATRIX.md`, `KAFE-MATRIX-REPORT.md`, `derived/kafe_matrix_controls.json`, the two summaries, `tools/kafe_matrix.py`, this file.
- [11] `FINAL-REPORT.md` corrected: data-state note; §1 (new can-claim bullet with both KAFE figures; "cannot claim strict recall" replaced by the 52-subject limit; withdrawn-column bullet; F1 recall named); §2.1 (full pipeline 22,747.5 comparable / Type 1 84.4 not; D9+S4ours strict 95.8%, strict-recall F1 would be 83.6%; D5 strict 83.3%; five 100%-precision rows strict 8.3–45.8%); §2.2 rewritten (48 of 48 / 46 of 46 cheaper than the full pipeline; still all below it without proxy init, 12,594.4; detection-only 30 of 48 / 28 of 46, labelled not comparable); §2.3 (C15/C16 strict 75.0%); §3.2 (D9's 9 in-39 abstentions = 2 pos + 7 neg); §4 legend, recall paragraph and ms-denominator row; §5.1 intro and table (unknown pos/neg and strict-recall cells for all 48 rows from the summary; KAFE row strict 100.0%, ms 22747.5, new timing scope); §7 item 2 replaced by the three-scope table, KAFE row of the 300 ms table; §8.3; §8.4 two new correction entries (evidence, 14→7 phase mapping table with per-phase agreement, cells moved); §9. 25.7 now appears only in §8.4 as the withdrawn figure. Files changed: `FINAL-REPORT.md`, this file.
- [12] `KAFE-HIGHLIGHTS.md` corrected: headline table gains strict recall and labels F1 as decided-recall; KAFE ms 22747.5 (full pipeline); "53× the per-button cost" → about a seventeenth; recall note rewritten; cost table rebuilt (full pipeline comparable, Type 1 alone not; 46 of 46 / 48 of 48 cheaper; 28 of 46 / 30 of 48 on detection alone); "257× faster" → five orders of magnitude; 19 decided-100% rows vs 6 strict-100% (D4, D9+S4u, D9-noS4, D10a, D10a+base, D10b); strict recall added to the precision and C-rule tables; strict-recall deltas added (C13 −15.7, C15 −19.9, C16 −22.4); caveat 5. Verified from the JSONL that C10–C16 abstain on exactly godaddy/thefreedictionary/wendys (C15/C16 also dmv_ca, negative). Final checks: table-cell comparison vs HEAD, where no TP/FP/FN/TN/precision/F1/Axcess-ms cell changed in any of the three reports; `git status` lists no `artifacts/` file and nothing under `src/audit/`; `derived/kafe_matrix.jsonl` unchanged at 52 lines; nothing committed. Files changed: `KAFE-HIGHLIGHTS.md`, this file.
